package eu.ase.bilet1e.json;

import android.os.AsyncTask;

import java.net.URL;

public class HttpManager extends AsyncTask<String, Void, String> {
    private URL url;

    @Override
    protected String doInBackground(String... strings) {
        if(strings == null){
            return null;
        }
        return null;
    }
}
